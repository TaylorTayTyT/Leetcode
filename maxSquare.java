import java.math.*;
class maxSquare {
    public static void maxSquare(char[][] matrix) {
        System.out.println("hi");
        int maxSquareLength = Math.min(matrix.length, matrix[0].length);
    }
}